# Core dependencies
fastapi~=0.115.14
uvicorn~=0.35.0
python-dotenv~=1.1.1
PyYAML~=6.0.2
loguru~=0.7.3

# Pipecat Voice AI Framework
pipecat-ai[daily,deepgram,cartesia,silero,websocket,google,openai,koala,elevenlabs]==0.0.98
pipecat-ai-small-webrtc-prebuilt

# HTTP client for LightRAG API (external RAG service)
httpx[http2]>=0.27.0  # HTTP/2 support for better performance

# Audio analysis for tone detection (kaiser_best resampling)
librosa>=0.10.0  # Audio feature extraction with high-quality resampling
numpy>=1.24.0    # Numerical operations + A2UI embedding cosine similarity

# Google Gemini for keyword extraction and text sentiment
google-generativeai>=0.8.0

# ---------------------------------------------------------------------------
# OPTIONAL — emotion detection (MSP-PODCAST wav2vec2). Disabled by default
# (server.emotion_detection_enabled: false). These pull ~3-4GB and ~800MB RAM,
# so they are intentionally NOT installed for lean deployments. Only install if
# you re-enable emotion detection:
#     pip install "torch>=2.0.0" "transformers>=4.30.0"
# A2UI template selection now uses the OpenAI embeddings API (no local model),
# so sentence-transformers is no longer required at all.
# ---------------------------------------------------------------------------
